<h1>Songs</h1>

<ul>
  <% @songs.each do |s| %>
    <% @artist_name = s.artist_name %>
    <% @song_title = s.title %>
    <li><%= link_to @artist_name + " - " + @song_title, song_path(s) %></li>
<ul>
  <% end %>
</ul>